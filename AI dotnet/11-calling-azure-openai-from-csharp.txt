========================================================================
GenAI-Powered .NET  ·  Tutorial 11 of 27
Title: Calling Azure OpenAI from C#
========================================================================

TASK
Read `newvision-tutorial-builder.md` in this repository and follow it EXACTLY —
all rules, the 21 canonical sections, the hard counts (>=15 quiz, >=15 exam,
>=15 flashcards, >=15 interview Q&A), full glossary + tooltips, and the Step-5
self-audit. Generate ONE tutorial as a newvision JSON content file using the
INPUTS below, write it to wwwroot/tutorials/calling-azure-openai-from-csharp.json, then self-verify and
print the audit table.

INPUTS
COURSE NAME : GenAI-Powered .NET
CATEGORY    : GenAI-Powered .NET Application Development
TOPIC       : Calling Azure OpenAI from C#
SLUG        : calling-azure-openai-from-csharp
LEVEL       : Intermediate
CODE LANG   : C#
PREV        : azure-openai-overview
NEXT        : function-calling-and-tools
SUBTOPICS   :
  - The chat completion API
  - The embeddings API
  - Key parameters: temperature and max tokens
  - Handling errors and retries safely
  - Building a .NET console app that sends a prompt and displays the response
  - Managing configuration and API keys

CONSTRAINTS
- Do NOT mention any company, client, employer, or training-provider name
  anywhere in the tutorial (no exceptions). Keep it vendor-neutral; only name the
  Microsoft / Azure / .NET technologies this topic is actually about.
- Cover EVERY subtopic in the body, and reflect each one in the glossary, the
  tooltips, and at least one quiz/exam question or flashcard.
- Set prev/next in the JSON to the PREV/NEXT slugs above so the 27 tutorials form
  one ordered learning path. Breadcrumbs: Home > Learners > GenAI-Powered .NET
  Application Development > Calling Azure OpenAI from C#.
- Code examples in C#; keep them correct and runnable in spirit. Do not invent
  APIs — if unsure about an exact signature, keep the example illustrative and say
  so in section 13.

FEATURES (added 2026-09-05 — applies to this and every prompt)
- Include at least 2 "animation" content blocks (step-based animated diagrams,
  format defined in newvision-tutorial-builder.md) at the most concept-heavy
  points of the body sections. Animations help learners see the flow.
- Every tooltip key must appear verbatim somewhere in the body text — the site
  shows hover tooltips on those words for quick recall.
