@page "/CSharpNew/03-git-and-github-assignment-use-case.html"
@{
    Layout = "_Layout";
    ViewData["Title"] = "C# New - Git & Github + Assignment + Use case";
    ViewData["ActiveMenu"] = "CSharpNew";
    ViewData["ActivePage"] = "Module03";
}
@section LeftMenu {
    @await Html.PartialAsync("_LeftMenu_CSharpNew", new { CurrentModule = "03" })
}

<style>
    .tt { position: relative; cursor: help; border-bottom: 1px dashed #0d6efd; }
    .tt::after { content: attr(data-tip); position: absolute; left: 0; bottom: 125%; background: #0d6efd; color: #fff; padding: 6px 8px; border-radius: 6px; opacity: 0; pointer-events: none; width: 240px; transition: opacity .2s; font-size: 0.9rem; }
    .tt:hover::after { opacity: 1; }
    .zoomable { transition: transform .25s ease; transition-delay: 3s; transform-origin: center; }
    .zoomable:hover { transform: scale(1.03); }
    .example-box { background: #f4f4f4; border: 1px solid #dcdcdc; border-radius: 8px; }
    .code-block { background: #1e1e1e; color: #e5e5e5; padding: 12px; border-radius: 10px; overflow-x: auto; }
    .toc-box { background: #eef6ff; border: 1px solid #cfe2ff; border-radius: 8px; }
    .ans pre.code { background: #1e1e1e; color: #f8f9fa; padding: 12px; border-radius: 8px; overflow-x: auto; }
</style>
<main class="container-fluid my-4">
    <a id="prev-subtopic"></a>
    <a id="next-subtopic"></a>
    <header class="mb-3">
        <h1 class="h3">Git & Github + Assignment + Use case (Module 03)</h1>
        <p class="lead zoomable">Welcome to the C# New course. We keep explanations simple, add short steps, and give you room to practice.</p>
        <p class="zoomable">This module follows the promise: <span class="tt" data-tip="We keep guidance gentle so you can try things fast.">gentle guidance</span>, <span class="tt" data-tip="You get tiny tasks after each idea.">tiny tasks</span>, and <span class="tt" data-tip="You will see how it helps projects.">project focus</span>.</p>
        <div class="alert alert-info">Mandatory and optional assessment based questions will be provided after each and every module completion</div>
    </header>
    <section class="toc-box p-3 mb-4">
        <h2 class="h5">Auto Table of Contents</h2>
        <ol>
            <li><a href="#m03-introduction-to-azure-repos-and-git-01">Introduction to Azure Repos and Git</a></li>
            <li><a href="#m03-installing-git-02">Installing Git</a></li>
            <li><a href="#m03-create-a-project-and-push-to-repo-03">Create a Project and Push to Repo</a></li>
            <li><a href="#m03-clone-a-project-from-repo-04">Clone a Project from Repo</a></li>
            <li><a href="#m03-giving-permissions-to-your-code-05">Giving permissions to your code</a></li>
            <li><a href="#m03-troubleshooting-proxy-issues-06">Troubleshooting Proxy Issues</a></li>
            <li><a href="#m03-working-with-branches-07">Working with Branches</a></li>
            <li><a href="#m03-save-work-with-commits-fetch-pull-and-sync-08">Save work with Commits, Fetch, Pull & Sync</a></li>
            <li><a href="#m03-link-commits-to-work-items-09">Link commits to work items</a></li>
            <li><a href="#m03-stage-files-and-use-gitignore-10">Stage files and use .gitignore</a></li>
            <li><a href="#m03-view-history-compare-and-revert-changes-11">View History, Compare & Revert changes</a></li>
            <li><a href="#m03-amend-previous-commit-12">Amend previous commit</a></li>
            <li><a href="#m03-handle-merge-conflicts-13">Handle Merge Conflicts</a></li>
            <li><a href="#m03-update-branch-from-master-before-pull-request-14">Update branch from master (before Pull Request)</a></li>
            <li><a href="#m03-create-pull-request-and-code-review-15">Create Pull Request & Code Review</a></li>
            <li><a href="#m03-apply-branch-policy-16">Apply Branch Policy</a></li>
            <li><a href="#m03-tags-17">Tags</a></li>
            <li><a href="#m03-cherry-pick-18">Cherry-Pick</a></li>
            <li><a href="#m03-fork-your-repo-19">Fork your repo</a></li>
            <li><a href="#m03-stash-your-code-20">Stash your code</a></li>
        </ol>
    </section>

<section id="m03-introduction-to-azure-repos-and-git-01" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Introduction to Azure Repos and Git</h3>
    </header>
        <p class="zoomable">Introduction to Azure Repos and Git is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Introduction to Azure ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Introduction to Az</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate introduction to azure repos and git to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#prev-subtopic">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-installing-git-02">Next</a>
    </div>
</section>


<section id="m03-installing-git-02" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Installing Git</h3>
    </header>
        <p class="zoomable">Installing Git is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Installing Git]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Installing Git</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate installing git to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-introduction-to-azure-repos-and-git-01">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-create-a-project-and-push-to-repo-03">Next</a>
    </div>
</section>


<section id="m03-create-a-project-and-push-to-repo-03" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Create a Project and Push to Repo</h3>
    </header>
        <p class="zoomable">Create a Project and Push to Repo is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Create a Project and P]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Create a Project a</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate create a project and push to repo to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-installing-git-02">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-clone-a-project-from-repo-04">Next</a>
    </div>
</section>


<section id="m03-clone-a-project-from-repo-04" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Clone a Project from Repo</h3>
    </header>
        <p class="zoomable">Clone a Project from Repo is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Clone a Project from R]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Clone a Project fr</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate clone a project from repo to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-create-a-project-and-push-to-repo-03">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-giving-permissions-to-your-code-05">Next</a>
    </div>
</section>


<section id="m03-giving-permissions-to-your-code-05" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Giving permissions to your code</h3>
    </header>
        <p class="zoomable">Giving permissions to your code is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Giving permissions to ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Giving permissions</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate giving permissions to your code to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-clone-a-project-from-repo-04">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-troubleshooting-proxy-issues-06">Next</a>
    </div>
</section>


<section id="m03-troubleshooting-proxy-issues-06" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Troubleshooting Proxy Issues</h3>
    </header>
        <p class="zoomable">Troubleshooting Proxy Issues is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Troubleshooting Proxy ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Troubleshooting Pr</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate troubleshooting proxy issues to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-giving-permissions-to-your-code-05">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-working-with-branches-07">Next</a>
    </div>
</section>


<section id="m03-working-with-branches-07" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Working with Branches</h3>
    </header>
        <p class="zoomable">Working with Branches is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Working with Branches]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Working with Branc</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate working with branches to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-troubleshooting-proxy-issues-06">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-save-work-with-commits-fetch-pull-and-sync-08">Next</a>
    </div>
</section>


<section id="m03-save-work-with-commits-fetch-pull-and-sync-08" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Save work with Commits, Fetch, Pull & Sync</h3>
    </header>
        <p class="zoomable">Save work with Commits, Fetch, Pull & Sync is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Save work with Commits]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Save work with Com</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate save work with commits, fetch, pull & sync to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-working-with-branches-07">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-link-commits-to-work-items-09">Next</a>
    </div>
</section>


<section id="m03-link-commits-to-work-items-09" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Link commits to work items</h3>
    </header>
        <p class="zoomable">Link commits to work items is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Link commits to work i]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Link commits to wo</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate link commits to work items to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-save-work-with-commits-fetch-pull-and-sync-08">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-stage-files-and-use-gitignore-10">Next</a>
    </div>
</section>


<section id="m03-stage-files-and-use-gitignore-10" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Stage files and use .gitignore</h3>
    </header>
        <p class="zoomable">Stage files and use .gitignore is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Stage files and use .g]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Stage files and us</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate stage files and use .gitignore to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-link-commits-to-work-items-09">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-view-history-compare-and-revert-changes-11">Next</a>
    </div>
</section>


<section id="m03-view-history-compare-and-revert-changes-11" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">View History, Compare & Revert changes</h3>
    </header>
        <p class="zoomable">View History, Compare & Revert changes is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[View History, Compare ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">View History, Comp</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate view history, compare & revert changes to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-stage-files-and-use-gitignore-10">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-amend-previous-commit-12">Next</a>
    </div>
</section>


<section id="m03-amend-previous-commit-12" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Amend previous commit</h3>
    </header>
        <p class="zoomable">Amend previous commit is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Amend previous commit]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Amend previous com</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate amend previous commit to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-view-history-compare-and-revert-changes-11">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-handle-merge-conflicts-13">Next</a>
    </div>
</section>


<section id="m03-handle-merge-conflicts-13" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Handle Merge Conflicts</h3>
    </header>
        <p class="zoomable">Handle Merge Conflicts is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Handle Merge Conflicts]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Handle Merge Confl</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate handle merge conflicts to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-amend-previous-commit-12">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-update-branch-from-master-before-pull-request-14">Next</a>
    </div>
</section>


<section id="m03-update-branch-from-master-before-pull-request-14" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Update branch from master (before Pull Request)</h3>
    </header>
        <p class="zoomable">Update branch from master (before Pull Request) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Update branch from mas]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Update branch from</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate update branch from master (before pull request) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-handle-merge-conflicts-13">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-create-pull-request-and-code-review-15">Next</a>
    </div>
</section>


<section id="m03-create-pull-request-and-code-review-15" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Create Pull Request & Code Review</h3>
    </header>
        <p class="zoomable">Create Pull Request & Code Review is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Create Pull Request & ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Create Pull Reques</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate create pull request & code review to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-update-branch-from-master-before-pull-request-14">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-apply-branch-policy-16">Next</a>
    </div>
</section>


<section id="m03-apply-branch-policy-16" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Apply Branch Policy</h3>
    </header>
        <p class="zoomable">Apply Branch Policy is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Apply Branch Policy]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Apply Branch Polic</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate apply branch policy to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-create-pull-request-and-code-review-15">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-tags-17">Next</a>
    </div>
</section>


<section id="m03-tags-17" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Tags</h3>
    </header>
        <p class="zoomable">Tags is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Tags]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Tags</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate tags to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-apply-branch-policy-16">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-cherry-pick-18">Next</a>
    </div>
</section>


<section id="m03-cherry-pick-18" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Cherry-Pick</h3>
    </header>
        <p class="zoomable">Cherry-Pick is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Cherry-Pick]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Cherry-Pick</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate cherry-pick to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-tags-17">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-fork-your-repo-19">Next</a>
    </div>
</section>


<section id="m03-fork-your-repo-19" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Fork your repo</h3>
    </header>
        <p class="zoomable">Fork your repo is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Fork your repo]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Fork your repo</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate fork your repo to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-cherry-pick-18">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m03-stash-your-code-20">Next</a>
    </div>
</section>


<section id="m03-stash-your-code-20" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Stash your code</h3>
    </header>
        <p class="zoomable">Stash your code is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Stash your code]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Stash your code</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate stash your code to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Diagnostics;
class GitFlow{
    static void Main(){
        var psi = new ProcessStartInfo("git", "status"){ RedirectStandardOutput=true };
        var proc = Process.Start(psi)!;
        Console.WriteLine(proc.StandardOutput.ReadToEnd());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m03-fork-your-repo-19">Prev</a>
        <a class="btn btn-primary btn-sm" href="#next-subtopic">Next</a>
    </div>
</section>

    
<section class="my-4">
    <h3>Glossary (Tooltips)</h3>
    <table class="table table-bordered">
        <thead><tr><th>Term</th><th>Tooltip Description</th></tr></thead>
        <tbody>
            <tr><td>CLI</td><td>Runs commands from the console to control tools.</td></tr>
            <tr><td>CLR</td><td>Manages memory and execution for C# apps.</td></tr>
            <tr><td>OOP</td><td>Organizes code into objects with data and behavior.</td></tr>
            <tr><td>LINQ</td><td>Query-like syntax for working with data in C#.</td></tr>
        </tbody>
    </table>
</section>

    
<section id="practice-coding-questions" class="my-5">
    <h2>To Become Best coder Practice this</h2>
    
    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 1: Git & Github + Assignment + Use case scenario 1</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 1 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample1 → Output sample1</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 2: Git & Github + Assignment + Use case scenario 2</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 2 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample2 → Output sample2</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 3: Git & Github + Assignment + Use case scenario 3</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 3 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample3 → Output sample3</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 4: Git & Github + Assignment + Use case scenario 4</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 4 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample4 → Output sample4</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 5: Git & Github + Assignment + Use case scenario 5</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 5 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample5 → Output sample5</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 6: Git & Github + Assignment + Use case scenario 6</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 6 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample6 → Output sample6</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 7: Git & Github + Assignment + Use case scenario 7</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 7 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample7 → Output sample7</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 8: Git & Github + Assignment + Use case scenario 8</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 8 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample8 → Output sample8</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 9: Git & Github + Assignment + Use case scenario 9</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 9 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample9 → Output sample9</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 10: Git & Github + Assignment + Use case scenario 10</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where git & github + assignment + use case concept 10 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample10 → Output sample10</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>
    <h3>Answers (Click to Expand)</h3>
    
    <details class="ans mb-2"><summary>Show Answer Code 1</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 1: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 2</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 2: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 3</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 3: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 4</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 4: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 5</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 5: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 6</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 6: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 7</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 7: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 8</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 8: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 9</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 9: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 10</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 10: Git & Github + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>
</section>

    <section class="my-4">
        <h3>FAQs</h3>
        <div class="mb-2"><strong>Q1:</strong> How does this help? <br /><span class="text-muted">A1:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q2:</strong> How does this help? <br /><span class="text-muted">A2:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q3:</strong> How does this help? <br /><span class="text-muted">A3:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q4:</strong> How does this help? <br /><span class="text-muted">A4:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q5:</strong> How does this help? <br /><span class="text-muted">A5:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q6:</strong> How does this help? <br /><span class="text-muted">A6:</span> It gives a quick reminder with a tiny actionable idea.</div>
    </section>
    <section class="my-4">
        <h3>Interview Questions</h3>
        <ul>
            <li>Interview Check 1: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 2: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 3: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 4: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 5: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 6: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 7: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 8: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 9: Explain the idea in 2-3 lines and give a code hint.</li>
        </ul>
    </section>
    <section class="my-4">
        <h3>MCQ</h3>
        
        <div class="mb-3">
            <p><strong>MCQ 1.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 2.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 3.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 4.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 5.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 6.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>
    </section>
    <section class="my-4">
        <h3>Summary</h3>
        <p class="zoomable">You learned the heart of Git & Github + Assignment + Use case and practiced with small scenarios to build confidence.</p>
    </section>
    <section class="my-4">
        <h3>Downloadable Resources</h3>
        <ul>
            <li>ZIP (placeholder)</li>
            <li>PPT (placeholder)</li>
            <li>DOC (placeholder)</li>
        </ul>
    </section>
</main>
