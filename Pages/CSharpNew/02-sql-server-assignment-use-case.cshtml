@page "/CSharpNew/02-sql-server-assignment-use-case.html"
@{
    Layout = "_Layout";
    ViewData["Title"] = "C# New - SQL Server + Assignment + Use case";
    ViewData["ActiveMenu"] = "CSharpNew";
    ViewData["ActivePage"] = "Module02";
}
@section LeftMenu {
    @await Html.PartialAsync("_LeftMenu_CSharpNew", new { CurrentModule = "02" })
}

<style>
    .tt { position: relative; cursor: help; border-bottom: 1px dashed #0d6efd; }
    .tt::after { content: attr(data-tip); position: absolute; left: 0; bottom: 125%; background: #0d6efd; color: #fff; padding: 6px 8px; border-radius: 6px; opacity: 0; pointer-events: none; width: 240px; transition: opacity .2s; font-size: 0.9rem; }
    .tt:hover::after { opacity: 1; }
    .zoomable { transition: transform .25s ease; transition-delay: 3s; transform-origin: center; }
    .zoomable:hover { transform: scale(1.03); }
    .example-box { background: #f4f4f4; border: 1px solid #dcdcdc; border-radius: 8px; }
    .code-block { background: #1e1e1e; color: #e5e5e5; padding: 12px; border-radius: 10px; overflow-x: auto; }
    .toc-box { background: #eef6ff; border: 1px solid #cfe2ff; border-radius: 8px; }
    .ans pre.code { background: #1e1e1e; color: #f8f9fa; padding: 12px; border-radius: 8px; overflow-x: auto; }
</style>
<main class="container-fluid my-4">
    <a id="prev-subtopic"></a>
    <a id="next-subtopic"></a>
    <header class="mb-3">
        <h1 class="h3">SQL Server + Assignment + Use case (Module 02)</h1>
        <p class="lead zoomable">Welcome to the C# New course. We keep explanations simple, add short steps, and give you room to practice.</p>
        <p class="zoomable">This module follows the promise: <span class="tt" data-tip="We keep guidance gentle so you can try things fast.">gentle guidance</span>, <span class="tt" data-tip="You get tiny tasks after each idea.">tiny tasks</span>, and <span class="tt" data-tip="You will see how it helps projects.">project focus</span>.</p>
        <div class="alert alert-info">Mandatory and optional assessment based questions will be provided after each and every module completion</div>
    </header>
    <section class="toc-box p-3 mb-4">
        <h2 class="h5">Auto Table of Contents</h2>
        <ol>
            <li><a href="#m02-database-and-dbms-fundamentals-01">Database & DBMS Fundamentals:</a></li>
            <li><a href="#m02-introduction-to-database-and-dbms-02">Introduction to Database and DBMS</a></li>
            <li><a href="#m02-types-of-dbms-relational-hierarchical-network-obje-03">Types of DBMS (Relational, Hierarchical, Network, Object-Oriented)</a></li>
            <li><a href="#m02-relational-database-advantages-and-disadvantages-04">Relational Database – Advantages & Disadvantages</a></li>
            <li><a href="#m02-acid-properties-and-dbms-properties-05">ACID Properties & DBMS Properties</a></li>
            <li><a href="#m02-ms-sql-introduction-and-setup-06">MS SQL Introduction & Setup:</a></li>
            <li><a href="#m02-overview-of-ms-sql-and-its-architecture-07">Overview of MS SQL and its Architecture</a></li>
            <li><a href="#m02-sql-server-installation-and-environment-setup-08">SQL Server Installation & Environment Setup</a></li>
            <li><a href="#m02-ms-sql-databases-and-tables-09">MS SQL Databases and Tables</a></li>
            <li><a href="#m02-filtering-sorting-and-functions-10">Filtering, Sorting & Functions:</a></li>
            <li><a href="#m02-filtering-records-where-like-in-between-11">Filtering Records (WHERE, LIKE, IN, BETWEEN)</a></li>
            <li><a href="#m02-sorting-and-grouping-order-by-group-by-having-12">Sorting and Grouping (ORDER BY, GROUP BY, HAVING)</a></li>
            <li><a href="#m02-aggregate-and-string-functions-13">Aggregate and String Functions</a></li>
            <li><a href="#m02-subqueries-set-operations-and-joins-14">Subqueries, Set Operations & Joins:</a></li>
            <li><a href="#m02-subqueries-single-row-multi-row-correlated-15">Subqueries (Single-row, Multi-row, Correlated)</a></li>
            <li><a href="#m02-set-operations-union-intersect-except-16">Set Operations – UNION, INTERSECT, EXCEPT</a></li>
            <li><a href="#m02-joins-inner-left-right-full-self-cross-17">Joins – INNER, LEFT, RIGHT, FULL, SELF, CROSS</a></li>
            <li><a href="#m02-database-objects-and-data-manipulation-18">Database Objects & Data Manipulation:</a></li>
            <li><a href="#m02-creating-tables-keys-and-constraints-19">Creating Tables, Keys, and Constraints</a></li>
            <li><a href="#m02-insert-update-delete-advanced-20">Insert, Update, Delete (advanced)</a></li>
            <li><a href="#m02-copy-records-from-another-table-21">Copy records from another table</a></li>
            <li><a href="#m02-views-create-modify-drop-22">Views – Create, Modify, Drop</a></li>
            <li><a href="#m02-schema-modifications-and-assignments-23">Schema Modifications & Assignments:</a></li>
            <li><a href="#m02-alter-table-add-drop-modify-columns-24">Alter Table – Add/Drop/Modify columns</a></li>
            <li><a href="#m02-manage-constraints-25">Manage Constraints</a></li>
            <li><a href="#m02-final-review-and-mini-project-26">Final Review & Mini Project</a></li>
        </ol>
    </section>

<section id="m02-database-and-dbms-fundamentals-01" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Database & DBMS Fundamentals:</h3>
    </header>
        <p class="zoomable">Database & DBMS Fundamentals: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Database & DBMS Fundam]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Database & DBMS Fu</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate database & dbms fundamentals: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#prev-subtopic">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-introduction-to-database-and-dbms-02">Next</a>
    </div>
</section>


<section id="m02-introduction-to-database-and-dbms-02" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Introduction to Database and DBMS</h3>
    </header>
        <p class="zoomable">Introduction to Database and DBMS is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Introduction to Databa]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Introduction to Da</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate introduction to database and dbms to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-database-and-dbms-fundamentals-01">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-types-of-dbms-relational-hierarchical-network-obje-03">Next</a>
    </div>
</section>


<section id="m02-types-of-dbms-relational-hierarchical-network-obje-03" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Types of DBMS (Relational, Hierarchical, Network, Object-Oriented)</h3>
    </header>
        <p class="zoomable">Types of DBMS (Relational, Hierarchical, Network, Object-Oriented) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Types of DBMS (Relatio]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Types of DBMS (Rel</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate types of dbms (relational, hierarchical, network, object-oriented) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-introduction-to-database-and-dbms-02">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-relational-database-advantages-and-disadvantages-04">Next</a>
    </div>
</section>


<section id="m02-relational-database-advantages-and-disadvantages-04" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Relational Database – Advantages & Disadvantages</h3>
    </header>
        <p class="zoomable">Relational Database – Advantages & Disadvantages is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Relational Database – ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Relational Databas</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate relational database – advantages & disadvantages to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-types-of-dbms-relational-hierarchical-network-obje-03">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-acid-properties-and-dbms-properties-05">Next</a>
    </div>
</section>


<section id="m02-acid-properties-and-dbms-properties-05" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">ACID Properties & DBMS Properties</h3>
    </header>
        <p class="zoomable">ACID Properties & DBMS Properties is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[ACID Properties & DBMS]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">ACID Properties & </text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate acid properties & dbms properties to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-relational-database-advantages-and-disadvantages-04">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-ms-sql-introduction-and-setup-06">Next</a>
    </div>
</section>


<section id="m02-ms-sql-introduction-and-setup-06" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">MS SQL Introduction & Setup:</h3>
    </header>
        <p class="zoomable">MS SQL Introduction & Setup: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[MS SQL Introduction & ]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">MS SQL Introductio</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate ms sql introduction & setup: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-acid-properties-and-dbms-properties-05">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-overview-of-ms-sql-and-its-architecture-07">Next</a>
    </div>
</section>


<section id="m02-overview-of-ms-sql-and-its-architecture-07" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Overview of MS SQL and its Architecture</h3>
    </header>
        <p class="zoomable">Overview of MS SQL and its Architecture is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Overview of MS SQL and]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Overview of MS SQL</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate overview of ms sql and its architecture to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-ms-sql-introduction-and-setup-06">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-sql-server-installation-and-environment-setup-08">Next</a>
    </div>
</section>


<section id="m02-sql-server-installation-and-environment-setup-08" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">SQL Server Installation & Environment Setup</h3>
    </header>
        <p class="zoomable">SQL Server Installation & Environment Setup is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[SQL Server Installatio]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">SQL Server Install</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate sql server installation & environment setup to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-overview-of-ms-sql-and-its-architecture-07">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-ms-sql-databases-and-tables-09">Next</a>
    </div>
</section>


<section id="m02-ms-sql-databases-and-tables-09" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">MS SQL Databases and Tables</h3>
    </header>
        <p class="zoomable">MS SQL Databases and Tables is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[MS SQL Databases and T]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">MS SQL Databases a</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate ms sql databases and tables to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-sql-server-installation-and-environment-setup-08">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-filtering-sorting-and-functions-10">Next</a>
    </div>
</section>


<section id="m02-filtering-sorting-and-functions-10" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Filtering, Sorting & Functions:</h3>
    </header>
        <p class="zoomable">Filtering, Sorting & Functions: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Filtering, Sorting & F]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Filtering, Sorting</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate filtering, sorting & functions: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-ms-sql-databases-and-tables-09">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-filtering-records-where-like-in-between-11">Next</a>
    </div>
</section>


<section id="m02-filtering-records-where-like-in-between-11" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Filtering Records (WHERE, LIKE, IN, BETWEEN)</h3>
    </header>
        <p class="zoomable">Filtering Records (WHERE, LIKE, IN, BETWEEN) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Filtering Records (WHE]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Filtering Records </text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate filtering records (where, like, in, between) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-filtering-sorting-and-functions-10">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-sorting-and-grouping-order-by-group-by-having-12">Next</a>
    </div>
</section>


<section id="m02-sorting-and-grouping-order-by-group-by-having-12" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Sorting and Grouping (ORDER BY, GROUP BY, HAVING)</h3>
    </header>
        <p class="zoomable">Sorting and Grouping (ORDER BY, GROUP BY, HAVING) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Sorting and Grouping (]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Sorting and Groupi</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate sorting and grouping (order by, group by, having) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-filtering-records-where-like-in-between-11">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-aggregate-and-string-functions-13">Next</a>
    </div>
</section>


<section id="m02-aggregate-and-string-functions-13" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Aggregate and String Functions</h3>
    </header>
        <p class="zoomable">Aggregate and String Functions is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Aggregate and String F]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Aggregate and Stri</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate aggregate and string functions to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-sorting-and-grouping-order-by-group-by-having-12">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-subqueries-set-operations-and-joins-14">Next</a>
    </div>
</section>


<section id="m02-subqueries-set-operations-and-joins-14" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Subqueries, Set Operations & Joins:</h3>
    </header>
        <p class="zoomable">Subqueries, Set Operations & Joins: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Subqueries, Set Operat]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Subqueries, Set Op</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate subqueries, set operations & joins: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-aggregate-and-string-functions-13">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-subqueries-single-row-multi-row-correlated-15">Next</a>
    </div>
</section>


<section id="m02-subqueries-single-row-multi-row-correlated-15" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Subqueries (Single-row, Multi-row, Correlated)</h3>
    </header>
        <p class="zoomable">Subqueries (Single-row, Multi-row, Correlated) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Subqueries (Single-row]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Subqueries (Single</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate subqueries (single-row, multi-row, correlated) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-subqueries-set-operations-and-joins-14">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-set-operations-union-intersect-except-16">Next</a>
    </div>
</section>


<section id="m02-set-operations-union-intersect-except-16" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Set Operations – UNION, INTERSECT, EXCEPT</h3>
    </header>
        <p class="zoomable">Set Operations – UNION, INTERSECT, EXCEPT is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Set Operations – UNION]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Set Operations – U</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate set operations – union, intersect, except to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-subqueries-single-row-multi-row-correlated-15">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-joins-inner-left-right-full-self-cross-17">Next</a>
    </div>
</section>


<section id="m02-joins-inner-left-right-full-self-cross-17" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Joins – INNER, LEFT, RIGHT, FULL, SELF, CROSS</h3>
    </header>
        <p class="zoomable">Joins – INNER, LEFT, RIGHT, FULL, SELF, CROSS is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Joins – INNER, LEFT, R]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Joins – INNER, LEF</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate joins – inner, left, right, full, self, cross to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-set-operations-union-intersect-except-16">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-database-objects-and-data-manipulation-18">Next</a>
    </div>
</section>


<section id="m02-database-objects-and-data-manipulation-18" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Database Objects & Data Manipulation:</h3>
    </header>
        <p class="zoomable">Database Objects & Data Manipulation: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Database Objects & Dat]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Database Objects &</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate database objects & data manipulation: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-joins-inner-left-right-full-self-cross-17">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-creating-tables-keys-and-constraints-19">Next</a>
    </div>
</section>


<section id="m02-creating-tables-keys-and-constraints-19" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Creating Tables, Keys, and Constraints</h3>
    </header>
        <p class="zoomable">Creating Tables, Keys, and Constraints is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Creating Tables, Keys,]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Creating Tables, K</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate creating tables, keys, and constraints to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-database-objects-and-data-manipulation-18">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-insert-update-delete-advanced-20">Next</a>
    </div>
</section>


<section id="m02-insert-update-delete-advanced-20" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Insert, Update, Delete (advanced)</h3>
    </header>
        <p class="zoomable">Insert, Update, Delete (advanced) is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Insert, Update, Delete]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Insert, Update, De</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate insert, update, delete (advanced) to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-creating-tables-keys-and-constraints-19">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-copy-records-from-another-table-21">Next</a>
    </div>
</section>


<section id="m02-copy-records-from-another-table-21" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Copy records from another table</h3>
    </header>
        <p class="zoomable">Copy records from another table is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Copy records from anot]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Copy records from </text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate copy records from another table to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-insert-update-delete-advanced-20">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-views-create-modify-drop-22">Next</a>
    </div>
</section>


<section id="m02-views-create-modify-drop-22" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Views – Create, Modify, Drop</h3>
    </header>
        <p class="zoomable">Views – Create, Modify, Drop is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Views – Create, Modify]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Views – Create, Mo</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate views – create, modify, drop to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-copy-records-from-another-table-21">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-schema-modifications-and-assignments-23">Next</a>
    </div>
</section>


<section id="m02-schema-modifications-and-assignments-23" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Schema Modifications & Assignments:</h3>
    </header>
        <p class="zoomable">Schema Modifications & Assignments: is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Schema Modifications &]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Schema Modificatio</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate schema modifications & assignments: to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-views-create-modify-drop-22">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-alter-table-add-drop-modify-columns-24">Next</a>
    </div>
</section>


<section id="m02-alter-table-add-drop-modify-columns-24" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Alter Table – Add/Drop/Modify columns</h3>
    </header>
        <p class="zoomable">Alter Table – Add/Drop/Modify columns is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Alter Table – Add/Drop]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Alter Table – Add/</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate alter table – add/drop/modify columns to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-schema-modifications-and-assignments-23">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-manage-constraints-25">Next</a>
    </div>
</section>


<section id="m02-manage-constraints-25" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Manage Constraints</h3>
    </header>
        <p class="zoomable">Manage Constraints is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Manage Constraints]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Manage Constraints</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate manage constraints to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-alter-table-add-drop-modify-columns-24">Prev</a>
        <a class="btn btn-primary btn-sm" href="#m02-final-review-and-mini-project-26">Next</a>
    </div>
</section>


<section id="m02-final-review-and-mini-project-26" class="mb-5">
    <header class="mb-2">
        <h3 class="h5">Final Review & Mini Project</h3>
    </header>
        <p class="zoomable">Final Review & Mini Project is introduced in plain language so you can connect it to daily tasks.</p>
        <p class="zoomable">We move step-by-step, showing inputs, outputs, and the small decisions in between.</p>
        <p class="zoomable">New words are softened with tooltips and short definitions to avoid jargon.</p>
        <p class="zoomable">You can safely test the idea in a console first before using it in a bigger app.</p>
    <p class="zoomable fw-semibold text-primary">Why this matters? It keeps your learning focused on real actions you can try right away.</p>

    <pre class="zoomable border rounded p-3 bg-body-secondary">[User Input]
   |
   v
[Final Review & Mini Pr]
   |
   v
[Clean Output]</pre>
    

    <figure class="zoomable my-3">
        <svg width="320" height="120" viewBox="0 0 320 120" xmlns="http://www.w3.org/2000/svg">
            <rect x="10" y="20" width="90" height="60" rx="10" fill="#e0f3ff" stroke="#007bff" />
            <rect x="120" y="20" width="90" height="60" rx="10" fill="#e8f5e9" stroke="#28a745" />
            <rect x="230" y="20" width="80" height="60" rx="10" fill="#fff3cd" stroke="#ffc107" />
            <line x1="100" y1="50" x2="120" y2="50" stroke="#007bff" marker-end="url(#arrow)" />
            <line x1="210" y1="50" x2="230" y2="50" stroke="#28a745" marker-end="url(#arrow)" />
            <text x="55" y="55" text-anchor="middle" font-size="12">Start</text>
            <text x="165" y="55" text-anchor="middle" font-size="12">Final Review & Min</text>
            <text x="270" y="55" text-anchor="middle" font-size="12">Result</text>
            <defs>
                <marker id="arrow" markerWidth="6" markerHeight="6" refX="5" refY="3" orient="auto" fill="#343a40">
                    <path d="M0,0 L6,3 L0,6 z" />
                </marker>
            </defs>
        </svg>
    </figure>
    
    <div class="example-box p-3 zoomable">Example: Relate final review & mini project to a tiny habit like logging a message, branching a repo, or running a quick query.</div>
    <pre class="code-block zoomable"><code>using System;
using System.Data.SqlClient;
class Sample{
    static void Main(){
        using var conn = new SqlConnection("Server=.;Database=Demo;Trusted_Connection=True;");
        conn.Open();
        using var cmd = new SqlCommand("SELECT TOP 1 GETDATE()", conn);
        Console.WriteLine(cmd.ExecuteScalar());
    }
}</code></pre>
    <p class="zoomable"><strong>Real-world use case:</strong> Teams rely on this idea to reduce manual steps and make releases calmer.</p>
    <p class="zoomable"><strong>Beginner tip:</strong> Write down what the inputs and outputs should be. <strong>Advanced note:</strong> Automate the check with a script or test.</p>
    <div class="d-flex justify-content-between mt-3">
        <a class="btn btn-outline-secondary btn-sm" href="#m02-manage-constraints-25">Prev</a>
        <a class="btn btn-primary btn-sm" href="#next-subtopic">Next</a>
    </div>
</section>

    
<section class="my-4">
    <h3>Glossary (Tooltips)</h3>
    <table class="table table-bordered">
        <thead><tr><th>Term</th><th>Tooltip Description</th></tr></thead>
        <tbody>
            <tr><td>CLI</td><td>Runs commands from the console to control tools.</td></tr>
            <tr><td>CLR</td><td>Manages memory and execution for C# apps.</td></tr>
            <tr><td>OOP</td><td>Organizes code into objects with data and behavior.</td></tr>
            <tr><td>LINQ</td><td>Query-like syntax for working with data in C#.</td></tr>
        </tbody>
    </table>
</section>

    
<section id="practice-coding-questions" class="my-5">
    <h2>To Become Best coder Practice this</h2>
    
    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 1: SQL Server + Assignment + Use case scenario 1</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 1 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample1 → Output sample1</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 2: SQL Server + Assignment + Use case scenario 2</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 2 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample2 → Output sample2</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 3: SQL Server + Assignment + Use case scenario 3</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 3 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample3 → Output sample3</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 4: SQL Server + Assignment + Use case scenario 4</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 4 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample4 → Output sample4</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 5: SQL Server + Assignment + Use case scenario 5</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 5 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample5 → Output sample5</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 6: SQL Server + Assignment + Use case scenario 6</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 6 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample6 → Output sample6</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 7: SQL Server + Assignment + Use case scenario 7</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 7 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample7 → Output sample7</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 8: SQL Server + Assignment + Use case scenario 8</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 8 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample8 → Output sample8</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 9: SQL Server + Assignment + Use case scenario 9</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 9 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample9 → Output sample9</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>

    <article class="practice-question mb-3 p-3 border rounded-3 bg-light">
        <h4>Question 10: SQL Server + Assignment + Use case scenario 10</h4>
        <p><strong>Problem:</strong> Explain and code a tiny scenario where sql server + assignment + use case concept 10 is applied.</p>
        <p><strong>Input/Output:</strong> Accept simple console input and print a short decision.</p>
        <p><strong>Example Input/Output:</strong> Input sample10 → Output sample10</p>
        <p><strong>Constraints:</strong> Keep it small, under 40 lines, with clear variable names.</p>
        <p><strong>Hint:</strong> Use a helper method and basic validation before processing.</p>
    </article>
    <h3>Answers (Click to Expand)</h3>
    
    <details class="ans mb-2"><summary>Show Answer Code 1</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 1: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 2</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 2: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 3</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 3: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 4</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 4: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 5</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 5: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 6</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 6: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 7</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 7: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 8</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 8: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 9</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 9: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>

    <details class="ans mb-2"><summary>Show Answer Code 10</summary>
        <pre class="code">using System;
class Demo{
    static void Main(){
        Console.WriteLine("Scenario 10: SQL Server + Assignment + Use case");
        string input = Console.ReadLine() ?? string.Empty;
        if(string.IsNullOrWhiteSpace(input)){Console.WriteLine("No data");return;}
        Console.WriteLine($"Processed: {input.Trim()}");
    }
}</pre>
    </details>
</section>

    <section class="my-4">
        <h3>FAQs</h3>
        <div class="mb-2"><strong>Q1:</strong> How does this help? <br /><span class="text-muted">A1:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q2:</strong> How does this help? <br /><span class="text-muted">A2:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q3:</strong> How does this help? <br /><span class="text-muted">A3:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q4:</strong> How does this help? <br /><span class="text-muted">A4:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q5:</strong> How does this help? <br /><span class="text-muted">A5:</span> It gives a quick reminder with a tiny actionable idea.</div>
<div class="mb-2"><strong>Q6:</strong> How does this help? <br /><span class="text-muted">A6:</span> It gives a quick reminder with a tiny actionable idea.</div>
    </section>
    <section class="my-4">
        <h3>Interview Questions</h3>
        <ul>
            <li>Interview Check 1: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 2: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 3: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 4: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 5: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 6: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 7: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 8: Explain the idea in 2-3 lines and give a code hint.</li>
<li>Interview Check 9: Explain the idea in 2-3 lines and give a code hint.</li>
        </ul>
    </section>
    <section class="my-4">
        <h3>MCQ</h3>
        
        <div class="mb-3">
            <p><strong>MCQ 1.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 2.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 3.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 4.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 5.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>

        <div class="mb-3">
            <p><strong>MCQ 6.</strong> Pick the safest option for this module.</p>
            <ul>
                <li>A) Quick guess</li>
                <li>B) Checked with docs</li>
                <li>C) Tested in code</li>
                <li>D) Peer reviewed</li>
            </ul>
            <p><em>Answer:</em> C</p>
        </div>
    </section>
    <section class="my-4">
        <h3>Summary</h3>
        <p class="zoomable">You learned the heart of SQL Server + Assignment + Use case and practiced with small scenarios to build confidence.</p>
    </section>
    <section class="my-4">
        <h3>Downloadable Resources</h3>
        <ul>
            <li>ZIP (placeholder)</li>
            <li>PPT (placeholder)</li>
            <li>DOC (placeholder)</li>
        </ul>
    </section>
</main>
