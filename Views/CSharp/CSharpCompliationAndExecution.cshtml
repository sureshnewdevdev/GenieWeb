@{
    ViewData["Title"] = "Compilation and Execution in C#.NET";
}

<!-- Section: Introduction -->
<section class="mb-5">
    <h2 class="text-secondary">Introduction</h2>
    <p class="lead">
        Understanding the **compilation and execution process** in C#.NET is crucial for developing efficient applications. This guide explains **how C# code is transformed into an executable program**, covering **compilation steps, execution flow, and important tools**.
    </p>
</section>

<!-- Section: Compilation Process -->
<section class="mb-5">
    <h2 class="text-secondary">Compilation Process in C#.NET</h2>
    <p>
        The **C# compilation process** involves multiple steps before the code is executed by the computer. Here’s a breakdown:
    </p>
    <ol class="list-group list-group-numbered">
        <li class="list-group-item">
            <b>Source Code (.cs file):</b> The developer writes C# code in a **.cs** file.
        </li>
        <li class="list-group-item">
            <b>Compilation to Intermediate Language (IL):</b> The C# compiler (<code>csc.exe</code>) **translates** the code into **Microsoft Intermediate Language (MSIL)**, which is platform-independent.
        </li>
        <li class="list-group-item">
            <b>Metadata Generation:</b> Information about **types, methods, and references** in the program is stored in the compiled assembly.
        </li>
        <li class="list-group-item">
            <b>Assembly Creation:</b> The **MSIL and metadata** are packaged into an **assembly** (<code>.exe</code> or <code>.dll</code>).
        </li>
    </ol>

    <p>
        This ensures that the code is checked for **syntax errors** and transformed into a format **understood by the .NET runtime**.
    </p>

    <h3 class="text-info">Diagram: C# Compilation Process</h3>
    <img src="~/images/csharp-compilation-process.png" alt="C# Compilation Process" class="img-fluid rounded shadow-sm" />
</section>

<!-- Section: Execution Process -->
<section class="mb-5">
    <h2 class="text-secondary">Execution Process in C#.NET</h2>
    <p>
        When a C#.NET application runs, the following steps take place:
    </p>
    <ol class="list-group list-group-numbered">
        <li class="list-group-item">
            <b>Loading the Assembly:</b> The **Common Language Runtime (CLR)** loads the compiled assembly into memory.
        </li>
        <li class="list-group-item">
            <b>Verification:</b> The CLR checks the **MSIL code** to ensure it is safe and does not contain **invalid instructions**.
        </li>
        <li class="list-group-item">
            <b>Just-In-Time (JIT) Compilation:</b> The MSIL is **converted into machine-specific native code** by the JIT compiler.
        </li>
        <li class="list-group-item">
            <b>Execution:</b> The **CPU executes the native code**, producing the program's output.
        </li>
    </ol>

    <h3 class="text-info">Diagram: Execution Process</h3>
    <img src="~/images/csharp-execution-process.png" alt="C# Execution Process" class="img-fluid rounded shadow-sm" />
</section>

<!-- Section: Example - Compiling and Running a C# Program -->
<section class="mb-5">
    <h2 class="text-secondary">Example: Compiling and Running a C# Program</h2>
    <p>
        Let's go through the **compilation and execution** of a simple **C# "Hello, World!"** program.
    </p>

    <h3 class="text-info">Sample C# Program:</h3>
    <div class="code-snippet position-relative bg-light p-3 rounded border">
        <pre class="mb-0">
<code>// HelloWorld.cs
using System;

class HelloWorld
{
    static void Main()
    {
        Console.WriteLine("Hello, World!");
    }
}</code>
        </pre>
        <button class="btn btn-outline-secondary copy-btn position-absolute top-0 end-0 m-2">Copy</button>
    </div>

    <h4>Compilation Steps:</h4>
    <ol class="list-group list-group-numbered">
        <li class="list-group-item">
            Open **Command Prompt** and navigate to the directory containing <code>HelloWorld.cs</code>.
        </li>
        <li class="list-group-item">
            Compile the program using the **C# compiler**:
            <pre class="bg-dark text-white p-2 rounded">csc HelloWorld.cs</pre>
            This generates an **executable file** named <code>HelloWorld.exe</code>.
        </li>
    </ol>

    <h4>Execution Steps:</h4>
    <ol class="list-group list-group-numbered">
        <li class="list-group-item">
            Run the compiled executable:
            <pre class="bg-dark text-white p-2 rounded">HelloWorld.exe</pre>
        </li>
        <li class="list-group-item">
            Output displayed:
            <pre class="bg-light p-2 rounded">Hello, World!</pre>
        </li>
    </ol>
</section>

<!-- Section: Tools Used in Compilation and Execution -->
<section class="mb-5">
    <h2 class="text-secondary">Tools Involved in Compilation and Execution</h2>
    <ul class="list-group">
        <li class="list-group-item">
            <b>C# Compiler (<code>csc.exe</code>):</b> Converts C# **source code** into **MSIL**.
        </li>
        <li class="list-group-item">
            <b>Common Language Runtime (CLR):</b> The execution engine that handles running **.NET applications**.
        </li>
        <li class="list-group-item">
            <b>Just-In-Time (JIT) Compiler:</b> Converts **MSIL to native code** at runtime.
        </li>
    </ul>
</section>

<!-- Section: Important Concepts -->
<section class="mb-5">
    <h2 class="text-secondary">Important Concepts in C# Compilation and Execution</h2>
    <ul class="list-group">
        <li class="list-group-item">
            <b>MSIL (Microsoft Intermediate Language):</b> A **CPU-independent** intermediate language used by .NET.
        </li>
        <li class="list-group-item">
            <b>Metadata:</b> Stores **type definitions, methods, and assembly references**.
        </li>
        <li class="list-group-item">
            <b>Assembly:</b> A compiled **code library** (<code>.exe</code> or <code>.dll</code>) used for **deployment and execution**.
        </li>
        <li class="list-group-item">
            <b>Managed Code:</b> Code that runs under the **.NET runtime**, benefiting from **garbage collection and security checks**.
        </li>
    </ul>
</section>

<!-- Section: Best Practices -->
<section class="mb-5">
    <h2 class="text-secondary">Best Practices for Compilation and Execution</h2>
    <ul class="list-group">
        <li class="list-group-item">
            Use **debugging tools** like **Visual Studio Debugger** to analyze runtime behavior.
        </li>
        <li class="list-group-item">
            Optimize performance by enabling **Release Mode Compilation** in Visual Studio.
        </li>
        <li class="list-group-item">
            Understand **JIT optimizations** and how the CLR executes code to write efficient applications.
        </li>
    </ul>
</section>

<!-- Navigation Buttons -->
<div class="d-flex justify-content-between mt-4">
    <a href="@Url.Action("FeaturesOfCSharp", "CSharp")" class="btn btn-outline-secondary">Previous: Features of C#</a>
    <a href="@Url.Action("GeneralStructureOfCSharp", "CSharp")" class="btn btn-outline-secondary">Next: General Structure of C# Program</a>
</div>

<!-- JavaScript for Copy Functionality -->
<script>
    document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            const code = this.previousElementSibling.innerText;
            navigator.clipboard.writeText(code).then(() => {
                alert('Code copied to clipboard!');
            });
        });
    });
</script>
