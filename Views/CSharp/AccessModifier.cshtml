@{
    ViewData["Title"] = "Access Modifiers in C#";
}

<!-- Section: Introduction to Access Modifiers -->
<section class="mb-5">
    <h2 class="text-secondary">What are Access Modifiers in C#?</h2>
    <p class="lead">
        Access modifiers in C# define the visibility and accessibility of classes, methods, and other members. They control how class members can be accessed from other classes and objects.
    </p>
</section>

<!-- Section: Types of Access Modifiers -->
<section class="mb-5">
    <h2 class="text-secondary">Types of Access Modifiers in C#</h2>
    <ul class="list-group">
        <li class="list-group-item"><b>public:</b> Accessible from any other class or assembly.</li>
        <li class="list-group-item"><b>private:</b> Accessible only within the same class.</li>
        <li class="list-group-item"><b>protected:</b> Accessible within its own class and by derived class instances.</li>
        <li class="list-group-item"><b>internal:</b> Accessible only within the same assembly.</li>
        <li class="list-group-item"><b>protected internal:</b> Accessible within its own assembly or from derived classes.</li>
        <li class="list-group-item"><b>private protected:</b> Accessible within its containing class or derived classes within the same assembly.</li>
    </ul>
</section>

<!-- Section: Example of Public Access Modifier -->
<section class="mb-5">
    <h2 class="text-secondary">Public Access Modifier</h2>
    <p class="lead">
        The <code>public</code> access modifier allows members of a class to be accessed from any other class or assembly.
    </p>

    <!-- Example Code Block -->
    <h3 class="text-info">Example of Public Access Modifier:</h3>
    <div class="code-snippet position-relative bg-light p-3 rounded border">
        <pre class="mb-0">
<code>public class Car
{
    public string Model;

    public void DisplayModel()
    {
        Console.WriteLine($"Model: {Model}");
    }
}

// Usage
Car car = new Car();
car.Model = "Toyota";
car.DisplayModel();  // Accessible from outside the class</code>
        </pre>
        <button class="btn btn-outline-secondary copy-btn position-absolute top-0 end-0 m-2">Copy</button>
    </div>

    <p class="text-muted">
        In this example, the <code>Model</code> field and <code>DisplayModel</code> method are marked as <code>public</code>, allowing them to be accessed outside the <code>Car</code> class.
    </p>
</section>

<!-- Section: Example of Private Access Modifier -->
<section class="mb-5">
    <h2 class="text-secondary">Private Access Modifier</h2>
    <p class="lead">
        The <code>private</code> access modifier restricts access to members within the same class only.
    </p>

    <!-- Example Code Block -->
    <h3 class="text-info">Example of Private Access Modifier:</h3>
    <div class="code-snippet position-relative bg-light p-3 rounded border">
        <pre class="mb-0">
<code>public class Car
{
    private string model;

    public void SetModel(string model)
    {
        this.model = model;
    }

    public void DisplayModel()
    {
        Console.WriteLine($"Model: {model}");
    }
}

// Usage
Car car = new Car();
car.SetModel("Honda");  // Can set model via public method
car.DisplayModel();  // Cannot directly access private member</code>
        </pre>
        <button class="btn btn-outline-secondary copy-btn position-absolute top-0 end-0 m-2">Copy</button>
    </div>

    <p class="text-muted">
        In this example, <code>model</code> is private, and it can only be accessed or modified via the public methods <code>SetModel</code> and <code>DisplayModel</code>.
    </p>
</section>

<!-- Section: Example of Protected Access Modifier -->
<section class="mb-5">
    <h2 class="text-secondary">Protected Access Modifier</h2>
    <p class="lead">
        The <code>protected</code> access modifier allows access within its own class and derived class instances.
    </p>

    <!-- Example Code Block -->
    <h3 class="text-info">Example of Protected Access Modifier:</h3>
    <div class="code-snippet position-relative bg-light p-3 rounded border">
        <pre class="mb-0">
<code>public class Vehicle
{
    protected string model;

    public void SetModel(string model)
    {
        this.model = model;
    }
}

public class Car : Vehicle
{
    public void DisplayModel()
    {
        Console.WriteLine($"Model: {model}");  // Accessible in derived class
    }
}

// Usage
Car car = new Car();
car.SetModel("Ford");
car.DisplayModel();</code>
        </pre>
        <button class="btn btn-outline-secondary copy-btn position-absolute top-0 end-0 m-2">Copy</button>
    </div>

    <p class="text-muted">
        In this example, <code>model</code> is protected, meaning it can only be accessed from within the <code>Vehicle</code> class or its derived class <code>Car</code>.
    </p>
</section>

<!-- Section: Key Points to Remember -->
<section class="mb-5">
    <h2 class="text-secondary">Key Points to Remember</h2>
    <ul class="list-group">
        <li class="list-group-item">Access modifiers control the visibility and accessibility of class members.</li>
        <li class="list-group-item"><code>public</code> members can be accessed from anywhere.</li>
        <li class="list-group-item"><code>private</code> members are accessible only within the class they are declared in.</li>
        <li class="list-group-item"><code>protected</code> members are accessible within their class and derived classes.</li>
        <li class="list-group-item">Other modifiers like <code>internal</code> and <code>protected internal</code> control access within the same assembly.</li>
    </ul>
</section>

<!-- Navigation Buttons -->
<div class="d-flex justify-content-between mt-4">
    <a href="@Url.Action("InstanceClassReferenceVariables", "CSharp")" class="btn btn-outline-secondary">Previous: Instance, Class & Reference Variables</a>
    <a href="@Url.Action("AbstractClasses", "CSharp")" class="btn btn-outline-secondary">Next: Abstract Classes</a>
</div>

<!-- JavaScript for Copy Functionality -->
<script>
    document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', function() {
            const code = this.previousElementSibling.innerText;
            navigator.clipboard.writeText(code).then(() => {
                alert('Code copied to clipboard!');
            });
        });
    });
</script>
